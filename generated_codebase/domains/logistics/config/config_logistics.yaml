# Logistics Domain Configuration
# Auto-generated by MEGA CODE GENERATOR

domain:
  name: logistics
  version: "1.0.0"
  description: "Configuration for logistics domain services"

agents:
  default:
    max_retries: 3
    timeout_seconds: 30
    batch_size: 100
    cache_ttl_seconds: 300
    confidence_threshold: 0.85
    quality_threshold: 0.90
    parallel_execution: true
    max_parallel_tasks: 10

services:
  api:
    host: "0.0.0.0"
    port: 8003
    workers: 4
    timeout: 30
    max_connections: 1000
    enable_cors: true
    enable_auth: true
    rate_limiting:
      enabled: true
      max_requests: 100
      window_seconds: 60

  grpc:
    host: "0.0.0.0"
    port: 9003
    max_message_size: 4194304
    keepalive_time: 30

  websocket:
    host: "0.0.0.0"
    port: 10003
    ping_interval: 25
    ping_timeout: 60

database:
  type: "postgresql"
  host: "localhost"
  port: 5432
  name: "logistics_db"
  pool_size: 20
  max_overflow: 10

cache:
  type: "redis"
  host: "localhost"
  port: 6379
  db: 3
  ttl_seconds: 300

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  handlers:
    - type: "console"
    - type: "file"
      path: "logs/logistics.log"
      max_bytes: 10485760
      backup_count: 5

monitoring:
  metrics:
    enabled: true
    port: 11003
    path: "/metrics"
  tracing:
    enabled: false
    sample_rate: 0.1
  health_check:
    enabled: true
    interval_seconds: 30

security:
  authentication:
    type: "jwt"
    secret_key_env: "LOGISTICS_JWT_SECRET"
    expiration_hours: 24
  encryption:
    algorithm: "AES-256-GCM"
    key_rotation_days: 90
